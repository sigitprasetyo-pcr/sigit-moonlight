<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Sigitmoonlight">

        <!-- Pertemuan 2 Activities -->
        <activity
            android:name=".pertemuan_2.FormulaActivity"
            android:exported="false" />

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".pertemuan_4.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Sigitmoonlight.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login & Auth -->
        <activity
            android:name=".pertemuan_11.onboarding.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Sigitmoonlight.Splash" />

        <activity
            android:name=".pertemuan_3.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_3.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_3.VerificationActivity"
            android:exported="false" />

        <!-- Main -->
        <activity
            android:name=".pertemuan_4.main.MainActivity"
            android:exported="false"
            android:launchMode="singleTop" />

        <activity
            android:name=".pertemuan_4.main.AuthActivity"
            android:exported="false" />

        <!-- Rumus -->
        <activity
            android:name=".pertemuan_4.rumus.RumusActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_4.welcome.WelcomeActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_4.common.DetailActivity"
            android:exported="false" />

        <!-- Pertemuan 5 Activities -->
        <activity
            android:name=".pertemuan_5.FifthActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_5.WebViewActivity"
            android:exported="false" />

        <!-- Pertemuan 9 Activities -->
        <activity
            android:name=".pertemuan_9.MenuActivity"
            android:exported="false" />

        <!-- Pertemuan 10 Activities -->
        <activity
            android:name=".pertemuan_10.tablayout.TabLayoutActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_10.recyclerview.DesaListActivity"
            android:exported="false" />

        <!-- Notification Receiver -->
        <receiver
            android:name=".notification.ReminderReceiver"
            android:exported="false" />

    </application>

</manifest>
